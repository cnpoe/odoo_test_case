<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <data>
        <record id="view_ilusiones_sale_order_form" model="ir.ui.view">
            <field name="name">sale.order.ilusiones.form</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form" />

            <field name="arch" type="xml">
                <xpath expr="//field[@name='order_line']" position="attributes">
                    <!-- <attribute name="attrs">{'readonly': [('state', 'in', ('done','cancel'))], 'invisible': [('combo_line', '=', True)]}</attribute> -->
                </xpath>
            </field>
        </record>
    </data>
</odoo>
